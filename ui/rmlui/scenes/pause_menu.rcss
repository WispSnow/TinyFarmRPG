#pause-panel {
    left: 216dp;
    top: 8dp;
    width: 160dp;
    height: 296dp;
    padding: 16dp;
}

#pause-message {
    height: 18dp;
    text-align: center;
    line-height: 18dp;
    font-effect: shadow(1dp 1dp #000000cc);
}

#pause-message.is-error {
    color: #ff4040ff;
}

#pause-message.is-success {
    color: #40ff40ff;
}

#pause-actions {
    display: flex;
    flex-direction: column;
    gap: 4dp;
}

.pause-action-button {
    width: 160dp;
    height: 30dp;
    line-height: 30dp;
}

#pause-values {
    margin-top: 8dp;
    display: flex;
    flex-direction: column;
    gap: 2dp;
}

.pause-value-row {
    width: 160dp;
    height: 30dp;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
}

#pause-values .tf-icon-button {
    width: 30dp;
    height: 30dp;
}
