# TinyFarmRPG 课程审阅进度

> 用途：跨会话追踪每一讲的阶段状态、用户确认、验证范围和残留 backlog。L00-L01 已按旧的一次性流程完成；L02 起按四阶段流程推进。

| 讲次 | 文件 | 当前阶段 | 状态 | 最近处理 | 代码状态 | 文本状态 | 验证 | Backlog |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| L00 | `lecture_plans/lectures/00-开篇.md` | 完成 | 旧流程完成 | 2026-05-30 | 无代码改动 | 已校准项目规模、技术栈版本、源码入口和自测提示 | 路径检查通过；源码入口存在；Mermaid 人工检查（`mmdc` 未安装） | 无 |
| L01 | `lecture_plans/lectures/01-游戏架构设计.md` | 完成 | 旧流程完成 | 2026-05-30 | 无代码改动 | 已校准 TinyFarm 规模、system 数量、装配顺序、ScriptHost/domain service 时序、ServiceLookup 口径和练习说明 | 路径检查通过；关键源码片段已核对；Mermaid 人工检查（`mmdc` 未安装）；`git diff --check` 通过 | 无 |
| L02 | `lecture_plans/lectures/02-领域服务与命令事件边界.md` | 完成 | 已完成 | 2026-05-30 | 已收紧 `InventoryDomainService::addItem()` catalog preflight；move/sort 写入收敛到 domain；`InventorySystem` 薄壳化 | 已校准 command / event 示例、`tf.command.add_item`、`InventoryChanged` 订阅者、SaveService 边界、`moveItem / sortInventory` 与排序 remap；同步更新 `docs/game/domain-services.md` | `ninja -C build/debug game_tests` 通过；L02 相关 76 项 CTest 通过；Markdown 相对链接检查通过；源码入口存在；已知过时表述 `rg` 检查通过；Mermaid 人工结构检查通过（`mmdc` 未安装）；`git diff --check` 通过 | 复合事务异常提交回滚策略已记入事实账本，后续 L10 / L11 / L13 复查 |
| L03 | `lecture_plans/lectures/03-RmlUi接入.md` | 完成 | 已完成 | 2026-05-30 | 已新增 `RmlUiRuntime::reloadDocument()`，失败保留旧文档；`reloadLastDocument()` 改走安全 reload；RmlUi Debug Panel 已支持对调试文档 Reload 并更新 document 指针；新增源码回归测试锁定 reload 顺序和 Debug Panel 入口；用户手动测试成功 | 已校准调试文档 Reload 与生产 `RmlDocumentController` 生命周期边界；资源段改为共享字体文件 / `stb_image` 解码栈、不共享 `ResourceManager / TextureManager` 纹理缓存；动态图片前缀修正为 `generated://`；补充鼠标优先输入口径、RmlUi Debug Panel 观察入口、阅读清单、源码入口、自测题和最小练习；同步更新 `docs/engine/ui_framework.md` 调试 reload 说明；未新增 Mermaid 图 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；RmlUi / 输入路由 / UI 布局相关 CTest 53/53 通过（2 项布局集成因环境跳过）；Markdown 相对链接检查通过；源码入口、阅读清单、练习路径和 `tools/rmlui_tester` 存在；过时表述 `rg` 扫描通过；Mermaid 人工结构检查通过（`mmdc` 未安装）；RML/RCSS 坑点 `rg` 扫描通过；`git diff --check` 通过 | L14/L22 复用 `generated://` 与字体加载边界 |
| L04 | `lecture_plans/lectures/04-HUD与覆盖式场景.md` | 完成 | 已完成 | 2026-05-30 | 已补测试护栏，无生产逻辑改动：锁定 `SceneManager::prepareUi()` 全栈调用 / 底层 `alpha=1.0`、覆盖式 Scene 输入上下文 push/pop，以及当前 `uiCoverage()` 策略；用户已确认进入阶段 4 收尾 | 已同步讲义、大纲与 docs 口径：`GameOverlay` / `GameInputPromptOverlay` 由 `GameScene` 直接持有；`prepareUi()` / `render()` 全栈调用且底层 `alpha=1.0f`；`ShopMenuScene` 隐藏底层 HUD；商店写回为 domain service + `InventoryChanged` / `HotbarChanged` + 自刷新，不存在 `WalletChangedEvent`；`GameMode` 只作为 L25 待复核接口线索 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；新增精确 CTest 6/6 通过；L04 相关 CTest 66 项执行通过、3 项 UI layout integration 因环境跳过；Markdown 相对链接检查通过；源码入口 / 阅读清单 / 练习路径存在；Mermaid 人工结构检查通过（`mmdc` 未安装）；过时口径 `rg` 扫描无残留；`git diff --check` 通过 | L25 复核 `GameMode` 真实使用口径 |
| L05 | `lecture_plans/lectures/05-输入上下文与菜单导航.md` | 完成 | 已完成 | 2026-05-30 | 已补齐 `BattleInputRouter` 对 `menu_confirm` / `menu_cancel` 的 150ms buffered press 消费：直接成功处理会清空同窗口 buffer，菜单状态从 `None` 恢复后可回放 buffered confirm/cancel；新增 3 个回归测试锁定回放与防重复触发；用户已确认进入阶段4收尾 | 已同步讲义、大纲与 `docs/engine/input_system.md`：Gameplay 下 `menu_up` 被过滤；`Menu` 允许 inventory/tab shortcuts 而 `Dialogue/Battle` 只允许 `menu_*`；HUD prompt 当前渲染 `fallback_text`，`icon_id` 作为 glyph 元数据保留；最小练习改为 `inventory` 的 I/B prompt；Rebind 捕获保留系统事件；GAMEPAD button down 仅 menu-like 上下文总是放行；战斗 InputBuffer 示例按真实 `BattleInputRouter` 路径讲解 | `ninja -C build/debug game_tests engine_tests` 通过；L05 输入/战斗路由/场景上下文/提示条相关 CTest 55/55 通过；Markdown 相对链接检查通过；源码入口 / 阅读清单 / 练习路径存在；已知过时表述 `rg` 扫描无残留；Mermaid 无反斜杠换行，`mmdc` 未安装，人工结构检查；`git diff --check` 通过 | L18 复核 `BattleInputRouter` 深讲边界；若后续实现真实 HUD 图标 glyph，再回改 L05 的 Action Prompt 段 |
| L06 | `lecture_plans/lectures/06-Lua内容层总览.md` | 完成 | 已完成 | 2026-05-30 | 已拆分 `ScriptHost` 初始化与 `bootstrap.lua` 执行：`GameScene::init()` 现在先完成读档 apply 或新游戏默认状态，再加载 bootstrap；暂停菜单读档成功后 reload bootstrap；移除 `tf.player.set_gold/add_gold` 写入口；新游戏初始 300 金改由 C++ 写入 `PlayerWalletComponent`；更新脚本装配/API 回归测试 | 已校准 L06 讲义、课程大纲 L06、`docs/tutorial/lua-content-authoring.md` 与 L07 最小跨讲示例：`tf.*` 改为 15 个只读子命名空间；`tf.time`/`lib.time`、`battle_ended` 注册入口、bootstrap 读档时序、暂停菜单 reload、新游戏 300 金来源和 `tf.player` 只读边界已同步；新增 bootstrap 时序 Mermaid；自测与最小练习已更新 | `ninja -C build/debug game_tests engine_tests` 通过；L06 相关 25 项 CTest 通过；`ctest --test-dir build/debug -R 'Script' --output-on-failure` 59/59 通过；`ctest --test-dir build/debug -R 'PauseMenuScene' --output-on-failure` 7/7 通过；Markdown 相对链接检查通过；源码入口 / 阅读清单 / 练习路径存在；Mermaid 结构检查通过（`mmdc` 未安装）；已知过时表述 `rg` 扫描无残留；`git diff --check` 通过 | 无 |
| L07 | `lecture_plans/lectures/07-ScriptHost与Sol2绑定.md` | 完成 | 已完成 | 2026-05-30 | 已让 `ScriptHost::reload()` 重新分配 `scene_token_`，清回调 / deferred command / module cache 后重新加载脚本，旧 `ScriptEntityHandle` 在 reload 后会被拒绝；`hardenLuaGlobals()` 已禁用 `collectgarbage`；新增 `ScriptHostLifecycleTest.ReloadInvalidatesOldHandles` 和沙箱断言；用户已确认进入阶段 4 收尾 | 已校准 L07 讲义、课程大纲与 `docs/tutorial/lua-binding-guide.md`：`tf.player.gold()` 读 `PlayerWalletComponent`；`tf.*` API 树补齐 15 个只读子命名空间、`tf.i18n` 与 `tf.player.gold()`；reload 语义改为同 VM 清事件回调 / deferred command / module cache 并推进 `scene_token_`；沙箱列表补 `collectgarbage`；完整数据流改为 `ScriptGameApi` + `triggerFromScript` + domain service；自测、最小练习构建命令和测试文件清单已更新 | `ninja -C build/debug engine_tests game_tests` 通过；`ctest --test-dir build/debug -R 'ScriptHost' --output-on-failure` 14/14 通过；`ctest --test-dir build/debug -R 'ScriptModuleRequire|Script' --output-on-failure` 60/60 通过；`ctest --test-dir build/debug -R 'PauseMenuScene' --output-on-failure` 7/7 通过；Markdown 相对链接检查通过；源码入口 / 阅读清单 / 练习路径存在；Mermaid 结构检查通过（`mmdc` 未安装）；已知过时表述 `rg` 扫描无残留；`git diff --check` 通过 | 无 |
| L08 | `lecture_plans/lectures/08-脚本事件桥与Tiled接入.md` | 完成 | 已完成 | 2026-05-30 | 无生产逻辑改动；已新增 `ScriptEventBridgeTest.InteractPayloadSharesLuaOwnedHandledFlagAcrossCallbacks`，锁定 `dialogue_handled` 初始不由 C++ 设置、同一次 `emitEvent()` 的 Lua 回调共享 payload table；已新增 `ScriptDialogueHelperTest.ChoiceHelperRoutesCancelResultToCallback`，锁定 DialogueChoice 取消结果回 Lua callback 的字段形态；用户已确认进入阶段 4 收尾 | 已同步 L08 讲义、课程大纲、`docs/tutorial/lua-content-authoring.md` 与 `docs/game/map_data_pipeline.md`：`dialogue_handled` 写为 Lua 脚本间协调标志；`tf.dialogue.choice` / `lib.dialogue.choice` 边界校准；Tiled 字段表补 `script_module`、object `name` 作为 blueprint key、辅助交互字段优先级 / 互斥约束；Blueprint 示例改为当前 `actor_blueprint.json` 形态 | `ninja -C build/debug engine_tests game_tests` 通过（no work to do）；L08 相关 CTest 28/28 通过；`ctest --test-dir build/debug -R 'Script' --output-on-failure` 62/62 通过；本阶段触达文件 Markdown 相对链接检查通过；源码入口 / 阅读清单 / 练习路径存在；Mermaid 结构检查通过（6 个块，`mmdc` 未安装）；讲义 / 教程 / 大纲陈旧表述 `rg` 扫描无残留；`git diff --check` 通过 | 无 |
| L09 | `lecture_plans/lectures/09-数据目录与RPG-Catalog.md` | 完成 | 已完成 | 2026-05-30 | 已收紧 catalog 加载原子性：`ContentCatalogLoader` 仅在加载/校验成功后发布 service 指针；`loadRpgCatalogFromManifest()` 使用临时 catalog 成功后替换；RPG classes/actors/skills/states/equipment/enemies/troops 与 `ItemCatalog` 均改为解析成功后提交；`ItemCatalog` 拒绝重复 item id / icon id；项目测试 helper 已补完整 `equipment` 加载 | 已同步 L09 讲义、大纲、`docs/game/data-catalogs.md` 与 `docs/game/audio_cue_catalog.md`：补“临时 catalog / 临时 map 成功后提交、失败不污染旧数据”的口径；行号式源码链接改为符号入口；AudioCue 阅读文档改为课程 L09；最小练习改为手动制造断引用并跑 catalog validation 测试，`rpg_importer` 退为离线导入进阶阅读 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；`ctest --test-dir build/debug -R 'RpgCatalog|RpgAssetsCatalog|ItemCatalog|QuestCatalog|ShopCatalog|AudioCueCatalog|GameSceneRuntimeAssembly|VfxBridgeSystem' --output-on-failure` 66/66 通过；`ctest --test-dir build/debug -R 'Catalog|RuntimeAssembly|ScriptPhase2Api|ScriptQuestFlow|ScriptRecruitmentFlow|ScriptShopFlow' --output-on-failure` 100/100 通过；触达文件相对链接检查通过；源码入口 / 阅读清单 / 练习路径存在；Mermaid 11 个块结构检查通过（`mmdc` 未安装）；旧口径 `rg` 扫描无残留；`git diff --check` 通过 | L09 无已知剩余风险；L10 / L11 / L13 / L16 复用 catalog 加载原子性事实 |
| L10 | `lecture_plans/lectures/10-任务系统.md` | 完成 | 阶段 4 已完成 | 2026-05-30 | 已新增 `InventoryDomainService::addItemsAtomically()`；`QuestTurnInService::turnIn()` 改为 reward item 批量原子写入成功后才加金币并替换 quest log；L10 项目 RPG catalog 测试 helper 改走完整 manifest loader；新增库存批量写入与任务交付失败不变更测试 | 已同步 L10 讲义 / 大纲 / `docs/gameplay/quest-system.md` / `docs/game/domain-services.md`：`turnIn()` 讲成 `next_quest_log` + `addItemsAtomically()` 的奖励原子提交；移除 warn/no rollback 旧口径；最小练习改为新增 `DefeatEnemyCount` objective，采集类 objective 作为进阶 C++ 扩展 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；L10 任务/库存/商店/装备邻近 CTest 77/77 通过；最终链接 / Mermaid 结构 / 源码路径 / stale wording / `git diff --check` 通过；`mmdc` 不可用，未做 Mermaid 渲染器验证 | L10 收口完成；L11 已完成；L13 已完成装备事务边界复核与文本同步 |
| L11 | `lecture_plans/lectures/11-商店系统.md` | 完成 | 阶段 4 已完成 | 2026-05-30 | 已将 `ShopTransactionService::commitBuy()` 改为重新 preview 后通过 `InventoryDomainService::addItemsAtomically()` 原子提交买入物品，成功后才扣金币；`previewBuy/Sell` 增加交易总价与卖出后钱包金额的 checked arithmetic，溢出时返回 `InvalidQuantity` 且不改背包 / 钱包；新增 4 个 ShopTransactionService 回归测试覆盖买入总价溢出、卖出总价溢出、卖出钱包溢出和部分容量不变更 | 已同步 `lecture_plans/lectures/11-商店系统.md`、`docs/gameplay/shop-system.md`、`docs/game/domain-services.md`、课程大纲 L11 与事实账本：商店买入口径改为 `addItemsAtomically()` atomic grant 成功后扣金币；补充 checked arithmetic / `InvalidQuantity` 溢出拒绝说明；`ShopMenuFocusArea` 补 `CategoryTabs`；阅读清单、源码入口、自测题和小结同步；用户已确认进入阶段 4 收口 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；L11 Shop / Inventory / ScriptShopFlow / ScriptPhase2Api 相关 CTest 74/74 通过；最终 Markdown 相对链接 29/29 通过；源码路径 182/182 通过；Mermaid 11 个块结构检查通过（`mmdc` 未安装）；旧口径 `rg` 无残留；`git diff --check` 通过 | L11 收口完成；L13 已完成装备事务边界复核与文本同步 |
| L12 | `lecture_plans/lectures/12-队伍与招募.md` | 完成 | 已完成 | 2026-05-30 | 已将 `PartyComponent::max_active_members_` 纳入 `PartyStateSaveData` 与 JSON `party_state.max_active_members`；`SAVE_SCHEMA_VERSION` 升至 8，新增 v7→v8 迁移并给旧存档补默认 4；`SaveService` 捕获 / 恢复队伍上限并在 normalize 时保证至少 1；新增满员招募只入花名册、运行时状态初始化、存档 roundtrip 与迁移回归测试 | 已同步 L12 讲义、`docs/gameplay/party-equipment-rest-recruitment.md`、`docs/game/save_and_flow.md`、`docs/overview.md`、L21 schema 口径和课程大纲：补 `kDefaultMaxActivePartyMembers`、`party_state.max_active_members`、schema v8 / v7→v8 迁移、满员只入花名册；新增队伍上限存档链路 Mermaid，更新阅读清单、源码入口、自测题和小结 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；L12 相关 `ctest --test-dir build/debug -R 'PartyRecruitmentSystem|RecruitmentInteractionSystem|RecruitOfferScene|ScriptRecruitmentFlow|ScriptPhase2Api|SaveData|SaveMigrator|SaveServiceAsync' --output-on-failure` 46/46 通过；Markdown 相对链接检查 6 文件通过；源码入口路径存在；Mermaid 15 个块人工结构检查通过（`mmdc` 未安装）；旧版 schema 文档口径扫描无残留；`git diff --check` 通过 | 无 |
| L13 | `lecture_plans/lectures/13-装备成长与休息.md` | 完成 | 已完成 | 2026-05-30 | 已将 `EquipmentDomainService::equipItem()` / `unequipItem()` 改为先在背包槽位副本中模拟，成功后一次写入 `InventoryComponent::slots_` 与 `PartyEquipmentComponent::loadouts_by_actor_id_`，再发 `InventoryChanged` / `EquipmentChanged`；替换 / 卸下的已装备物品若不在 `ItemCatalog`，提交前拒绝且保持状态不变；`EquipmentTabContent` 摘要改用 `PartyRuntimeStatsComponent.total_exp` 经 `ActorProgressionService::normalizeState()` 推导当前等级再解算属性；新增装备事务事件顺序、未知旧装备不变更和 Equipment 页当前等级源码护栏测试 | 已同步 L13 讲义、大纲、`docs/gameplay/party-equipment-rest-recruitment.md`、`docs/game/domain-services.md`、`docs/game/battle-internals.md` 与 `docs/game/runtime-assembly.md`：装备事务改为槽位副本预演后一次提交；Equipment 页摘要讲当前等级来源；休息事件边界改为 `RestSystem` 派发；战斗文档改为 8 项属性；移除旧的装备限制 / 事件命名 / 休息 Lua 写入口口径 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；L13 相关 `ctest --test-dir build/debug -R 'EquipmentDomainService|ActorProgressionService|PartyRestService|InventoryMenuPartyPanel|InventoryMenuSceneSlotGridRegistration|BattleUnitFactory|ItemUseSystem|SaveServiceAsync' --output-on-failure` 62/62 通过；Markdown 相对链接 40/40 通过；Mermaid 19 个块结构检查通过（`mmdc` 未安装）；源码入口 / 阅读清单 / 练习路径存在；已知陈旧表述 `rg` 扫描无残留；`git diff --check` 通过 | 无 |
| L14 | `lecture_plans/lectures/14-分层角色外观与头像.md` | 完成 | 已完成 | 2026-05-30 | 已补测试护栏，无生产逻辑改动：`applySelectionToEntity()` 批量写后只发 1 次 `RefreshAppearanceCommand`，无效目标不发刷新；`PlayerPortraitService` 对相同玩家外观事件保持 generated image URI / registration 稳定 | 已同步 L14 讲义、课程大纲、`docs/gameplay/layered-appearance.md` 与 `docs/game/runtime-assembly.md`：把“唯一桥”校准为 `AppearanceLayerCacheBuilder` 无状态缓存构建入口 + `AppearanceSystem` 探索态 command/event 壳；补 generated image 头像刷新边界与相同外观 registration 稳定性；最小练习改成给已有槽新增部件变体 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；L14 相关 CTest 79/79 通过；最终 Markdown 相对链接 26/26 通过；Mermaid 17 个块结构检查通过（`mmdc` 未安装）；源码入口 / 阅读清单 / 练习路径存在；旧口径 `rg` 扫描无残留；`git diff --check` 通过 | 无 |
| L15 | `lecture_plans/lectures/15-探索与战斗的过渡.md` | 完成 | 已完成 | 2026-05-30 | 已补代码护栏：`GameScene::onEnterBattleCommand()` 使用 `battle_in_progress_` 拒绝已有战斗未结束时的新战斗请求，地图遭遇被拒时释放 encounter；`BattleStartedEvent.actor_ids` 改为解析后的实际玩家 actor ids，预构建单位路径从 player unit source actor 回填；新增 GameScene battle entry 与 ScriptEventBridge payload 回归测试；用户已确认进入阶段 4 收尾 | 已同步 L15 讲义、课程大纲、`docs/gameplay/turn-based-battle.md`、`docs/game/interaction_and_dialogue.md`、`docs/game/system_scheduler.md`、`docs/game/battle-internals.md` 与 `docs/tutorial/lua-binding-guide.md`：补防重入流程、实际 actor ids、Tiled 遭遇触发器、`GameMode` profile 词汇表和战斗库存写回口径 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；L15 相关 CTest 70/70 通过；最终 Markdown 相对链接 33/33 通过；Mermaid 24 个块结构检查通过（`mmdc` 未安装）；源码入口 / 阅读清单 / 练习路径 17/17 存在；旧口径 `rg` 扫描无残留；`git diff --check` 通过 | 无 |
| L16 | `lecture_plans/lectures/16-回合制战斗领域核心.md` | 完成 | 已完成 | 2026-05-30 | 已完成阶段 2 代码修订：`TurnCore` 增加 forced outcome 标记，`Escaped` 不会被 `refresh()` / `advanceTurn()` 重算覆盖；`GameScene::onBattleEnded()` 在 HP/MP 出场写回变更后触发 `PartyRuntimeStatsChanged{full_sync=true}`；普通攻击 `BattleActionResult.damage` 改为实际扣掉的 HP；新增 `TurnCore`、`BattleActionResolver` 与 `GameSceneBattleEntry` 回归测试 | 已同步 L16 讲义、课程大纲、`docs/gameplay/turn-based-battle.md` 与 `docs/game/battle-internals.md`：补强制逃跑终局保持、战后 HP/MP 写回事件、普攻 damage 实际扣血语义；最小练习保留 `BothSidesWipedCountsAsDefeat` 作为新增测试练习 | `ninja -C build/debug game_tests engine_tests` 通过（no work to do）；精确回归 `TurnCoreTest|BattleActionResolverTest|GameSceneBattleEntryTest` 35/35 通过；L16/L17 邻近 CTest 76/76 通过；Markdown 相对链接 26/26 通过；源码路径 376/376 通过；Mermaid 15 个块结构检查通过（`mmdc` 未安装）；触达文件旧口径 `rg` 扫描无残留；`git diff --check` 通过 | L17 / L20 复用事实账本中的战斗动作结果与结算写回口径 |
| L17 | `lecture_plans/lectures/17-战斗动作解析.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L18 | `lecture_plans/lectures/18-战斗Action生成.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L19 | `lecture_plans/lectures/19-战斗表现与动画导演.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L20 | `lecture_plans/lectures/20-战斗结算与探索态写回.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L21 | `lecture_plans/lectures/21-存档系统与Schema迁移.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L22 | `lecture_plans/lectures/22-本地化与用户设置.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L23 | `lecture_plans/lectures/23-Effekseer与VFX管线.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L24 | `lecture_plans/lectures/24-异步地图预加载与主线程命令队列.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L25 | `lecture_plans/lectures/25-SystemScheduler与并行岛.md` | 阶段 1 | 未开始 |  |  |  |  |  |
| L26 | `lecture_plans/lectures/26-调试测试与课程收尾.md` | 阶段 1 | 未开始 |  |  |  |  |  |
